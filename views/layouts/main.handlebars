<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Techomode Blog</title>
    <!--May change font later, not sure about this one yet, so making a note-->
    <link href="https://fonts.googleapis.com/css?family=IBM+Plex+Sans:400,400i,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles/style.css">
</head>
<header>  
      <nav class="navbar">
    <ul>
      <li class="li-inline nav-item"><a href="/">Home</a></li>
      <li class="li-inline nav-item"><a href="/dash">Dash</a></li>
      {{#if loggedIn}}
      <li class="li-inline nav-item"><a href="#" id="logout-link">Logout</a></li>
      {{else}}
      <li class="li-inline nav-item"><a href="/login">Login</a></li>
      {{/if}}
    </ul>
  </nav>  
<body>
    <h1>Techomode</h1>
    <div class="container">
    {{{body}}}
    </div>
    <!-- should be only script necessary in handlebars main because login/logout buttton will only be visible interactivity until other hbs loaded-->
<script src="..\scripts\logout.js"></script>
</body>
</html>